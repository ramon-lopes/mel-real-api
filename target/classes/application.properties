# Porta (Railway injeta a variável PORT automaticamente)
server.port=${PORT:8080}

# Estoque máximo por dia (você muda aqui sempre que quiser)
estoque.limite-diario=30

# CORS — coloque aqui o domínio do seu site quando estiver em produção
# Durante desenvolvimento, "*" libera tudo
cors.allowed-origins=*
